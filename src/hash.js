

function hash(object_name){
    if( object_name === "andrew"){return 1}
    else if(...){return 0}
}

users = []

user = [1000]

user.forEach(element => {
    element.name
});
let someone = input()

user_index = hash(someone)
user_index[356]

users[user_index]








let sum=0
function add(a){
    if(a){
        sum+=1
        add(a-1)
    }
}
add(5)
console.log(sum)